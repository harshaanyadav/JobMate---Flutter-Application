name: jobmate
description: Resume Analyzer and Job Matcher App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^10.3.10
  syncfusion_flutter_pdf: ^33.1.46
  fl_chart: ^1.2.0
  provider: 6.1.5+1
  path_provider: 2.1.5
  screenshot: ^3.0.0
  share_plus: ^12.0.2
  google_fonts: ^8.0.2
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true